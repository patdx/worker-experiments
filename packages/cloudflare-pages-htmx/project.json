{
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["vite build --watch", "wrangler pages dev ./dist"],
        "cwd": "packages/cloudflare-pages-htmx"
      }
    }
  }
}
