{
  "name": "cloudflare-pages-htmx",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "vite build --watch",
          "wrangler pages dev ./dist --d1=DB --persist"
        ],
        "cwd": "packages/cloudflare-pages-htmx"
      }
    }
  }
}
