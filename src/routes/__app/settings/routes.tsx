import { ROUTE_LOG } from '../../../../lib/all-routes';

const SettingsPageRoutes = () => {
  return (
    <div className="p-2">
      <pre>{ROUTE_LOG}</pre>
    </div>
  );
};

export default SettingsPageRoutes;
